All right. Last time we talked, we talked about the importance of using, just like any other piece of structure in our program, how we structure the functions, how we structure data, how we structure channels conveys meaning about the structure of our program and how we intend it to behave. and this requires thinking pretty carefully about program structure and this is a thing that's actually not easy to do and it's not something that we actually teach particularly directly and I'm not sure even how I would other than for example diagramming things out like there are a couple of my IAs use diagramming tools to diagram the logical structure of their program. That's one way of actually getting to this, and it's pretty helpful. I think there might be a post on Piazza on it, if not, we'll make sure there is one. Because it helps your reader understand your thought process, and that reader might be you, you know, down the road. We also talked about a model of using channels to provide something called data passing. And in data passing, we have a bunch of coordinated, a bunch of go routines that are coordinating on some job and they pass the data necessary to perform the job back and forth between themselves. One of the nice things about data passing is that there's no shared mutable state. So it's completely impossible for two go routines to touch the same piece of memory concurrently through a pointer in a way that can't be reasoned about. So that eliminates a bunch of really difficult to find and fix bugs, but it still requires us a reason about ownership, because logically, there's only one copy of this state, and so we need to somehow track in our heads, and as we're writing this program, whether this particular Go routine owns the copy of the state or not. And that still leaves us open to a number of potential errors that are, and they're not as hard to fix as shared memory errors, but they're still unpleasant. So, we started building an idiom called lexical confinement and you can implement lexical confinement in two ways. The way I'm showing you in lecture is with function shipping. What's cool about function shipping is that you build this confined piece of state completely generically and you can apply any function to it at all. That's a little unusual if you've never used function shipping before or never used a language with functions as first class arguments or never really used functors. That's a little bit weird. So there is another way of doing this and we're going to show you an example. Once we finish building this example out today, we'll post an example of using it with instead of shipping actual functions, you encode all of the functions together with the confined state and then you basically ship RPC requests to it. So, you send a little bit of data to it that says which function to operate on and where to put the result. But either way, whether you're implemented with function shipping or essentially as remote procedure call, either way, a single entity, a single Go routine, owns and exclusively names the state in question. So, the basic idea is only one Go routine can ever utter the name of the state, which means no one else can ever touch it directly. The only way they can touch it is either through sending a function to the Go routine that owns it, or asking the Go routine to run one of its predetermined functions on its behalf. Today we're gonna finish building out that confinement example. That's probably gonna be about the first third of lecture, maybe a little more. And then we're gonna introduce the notion of consistency. Now consistency is just the set of promises made by the system about how operations on data behave. And you would think that's not that complicated, but it turns out to be one of the central topics of the course. And what we'll see is that sort of your model of computation so far is that you perform an operation, you write a piece of operation, when that line of code executes, the operation happens. Before the line of code executes, the operation hasn't happened. after that line of code executes, the operation has happened and everybody can agree on the timeline in which things occur. One of the interesting things about the confinement example is that it's taking concurrency and reducing it to something very similar to that, where everyone can agree there's a before and after of every single operation on this state, even though multiple go routines are trying to modify it at once. Which makes things very easy to reason about. The problem is, is that in distributed systems, it's impossible. And the reason it's impossible is because we're sending messages back and forth that both take measurable time and can be lost. And because of that, no one can ever agree precisely on when something happens. And that makes the set of promises made by the system of, you ask it to do an operation, when does that operation happen, that actually turns out to be complicated. And it's complicated even if we have a single server that isn't replicated. It gets even worse when we start replicating data, which we're going to need to do. But even if we just have a single server, it turns out to be difficult. So today I'm going to introduce the notion of linearizability. So you can think of consistency as existing across a spectrum from the weakest possible guarantees to the strongest possible guarantees. And if we're talking about updates on a single data element, the variable X, the strongest consistency model, the strongest set of guarantees we can possibly provide are linearizability. And you'll sometimes see people describe that as strong consistency. Now as a side, I'll just introduce the weakest is eventually everyone will agree that an operation has happened. And that's all you get. That turns out to still be kind of useful, but not as useful as linearizability. Basically, linearizability is the distributed system's version of making it look like every event happens in a total order and instantaneously. Okay, before I get started on today's material, does anyone have any questions about anything we've talked about up until this point? Yes. So that weakest model that eventually everyone agrees that an operation happened, is that like just the basic consensus problem? No, consensus is everyone agrees that it happened in an order. The eventual consistency is that if I submitted an operation to the system and nothing else happens, there is a point in the future at which everyone will know that it happened. but that point in the future is unbounded. So it's finite. There is a point somewhere in the, if we roll time forward long enough, eventually everyone will know that it, agree that it happened. But we can't say anything about how long that takes. We also can't say anything about the order in which people see things happen. Other questions? Okay. So, let's remind ourselves what this thing looked like. So we have this confined integer. The confined integer is represented by an abstract data type of value. The value holds within it an integer value. We can construct them with the zero value to begin. We can add offsets to it, positive or negative offsets. We can get the current value and we can tell that we can destroy the value that we've created. It's a pretty straight a pretty simple data structure that data structure is represented by two channels one on which we can send requests and requests are just functions with an argument that is a pointer to int and it can return anything it wants and then the responses are the responses of the channel through which those return values are discovered. We've looked at how to create an instance. We haven't looked, we don't know exactly how the instance does its work yet, but the way we create an instance is while we declare one of these things, we create a double-ended channel called requests and another double-ended channel called responses of the appropriate type. We assign requests and responses to the corresponding fields in the value struct which limits the external client's ability to read or write to them. Then we launch this closure. This closure just goes off and runs for as long as it runs until it's done and it does something. We'll talk about what that something is and once the closure is launched it returns the result. So the closure basically, the basic idea behind the closure is that it's going to read requests off of the request channel, apply them to its local state, take whatever return value is returned and send that return value on the response channel. And it's just gonna keep doing that until you destroy the value. All right, so now, so that's the part, that's sort of what we were talking about last time. This is the part where we're new, so we haven't seen this bit yet. All of the methods follow the same pattern. They're gonna declare a function that implements the work of the method as a closure. Then it's going to pass that function to the value closure as a request. Then it will, the value closure will do its work, it will send the response over the response channel, then it reads and returns the response. Those second two pieces are common to all of the methods And this is going to be, this is a little bit extreme, but one of the things, one of my very biggest pet peeves about code, is if you keep typing the same thing over and over and over again, that should have been a function. Now this is two lines, okay, it's a little extreme, but I'm still going to do it just as a sort of as an illustration. So we're going to abstract those last two steps away into this method on values called perform. Perform takes the function as a request that returns an any, sends that function to the request channel, waits for the responses channel to yield a response. Pretty straight forward. Alright, let's talk about how we build add. So add needs to do two things. It needs to define a function that will perform the work of an add and then call the operation on it. So the function that we're going to define and this is going to be true for every method function we ever build on this value because this is the type of a request. A request is a function that takes a pointer to integer as an argument and returns something of type any as a result. Well, inside that generic type signature, we have to decide what we're going to do. Well, the what we're going to do is pretty straightforward. We're going to take the argument that was passed to add, which is delta. We're going to add it to whatever the internal representation happens to be currently. and store it in the internal representation. So, one of the things to notice is that this delta is this version of the variable. So, this is why we're using a closure. So, the closures are written as a single argument on the state. If you want to have more arguments to the function that you're actually implementing, you implement it as one of these closures. This is also sometimes called a curried function. And that's because there's a person by the name of Curry who invented this idea that if you want to take a function of two arguments, you can create it by having two arguments of one function where one is nested inside the other. All right, sorry about the build there, that was awkward. So, requestor functions is one argument, they capture any other parameters in the closure. We don't care about the return value because add doesn't return anything. So because add doesn't return anything, the only thing we care about is that the work of the add is done before add returns. So it just returns nil, don't care about it. And then we call perform, and likewise we're going to ignore the return value of perform. Do we all have return-ill explicitly split functions? Yes, because the function is declared to return a value. So it has to return something. There's no implicit return? No, Go does not have an implicit return value. So if you left that line of code out, it wouldn't compile. because func has to return a value of type any without a return function there's no return value without a return value you can't compile the function there's no void return type well that's any any is anything just like void is anything and you can return any value because any value would be would satisfy the any value nil is convenient because nil is Nothing. And then we invoke the operation, that's add. So add is going to pass this function with the value of delta bound by the outer most argument to the closure, the closure will implement, will apply this function to its copy of the integer and then return nothing as a result. Okay, the get method is going to do something similar, right? So, get is going to define a function that takes a pointer to an integer and returns something of type any, and what it wants to do is return the current value that's stored inside the closure, so return pointer to value. Now this is a little awkward because the return value of the closure is of type any and the expression pointer to val is of type int. When operation returns, all we know that operation is giving us is that it's of type any. So, we call perform, it applies to the function, this anonymous function that's in the closure, that function returns an integer because val is a pointer to an integer. So we, but all we know when we get back the value from perform is that it is of type any. Because the compiler doesn't know that that return value is the thing that perform is going to return. And so we have to do this dynamic type checking bit by saying look I believe that the thing that I'm getting back from perform is going to be an int. Please when you're running this line of code check and verify that for me, and then return that. So because if this check works correctly, the type of that entire expression is an int, which means it meets the type requirement of the return value of get. I wouldn't miss this, but fn, we can't just have the return type of fn be int, or does it need to be a function? No, because if the functions, if the return type of fn were int, it would not type check to the other, to the function, that the function has to type check precisely. So perform requires a function that returns int. Yeah, and even if it, and even, and even if it, even if you said it returned an int, perform is declared to return any, so you still wouldn't know. All right, what about done? Done is very straightforward. All done does is close the request channel. So closing the request channel is, this is often called the termination idiom. Closing a request channel is signaling to the person receiving the requests that its work is done and it can finish. So, this is a very common thing. In fact, you'll use this in your projects from time to time. Closing a channel, closing a request channel or equivalently a termination channel, in our case, is going to notify the confining go routine that it's done. In response, the confining go routine can exit. That does two really important things. One is that the resources occupied by that Go routine are freed up. But also, that Go routine is the only thing that holds a long-standing reference to the confined state, so that confined state is also freed. So, Make allocates the confined state and allocates the resources to run a Go routine. Done deallocates all of that stuff. It doesn't actually deallocate it directly because Go is garbage collected. So, eventually the Go runtime will discover that the resources needed by this Go routine are no longer needed and it will return them to the free pool. Questions so far? Yes. What's the v.value if that's for all the function names? Like, are these functions that are outside of the strong percentage? v.value? Like, func v.value? So, what this means is that done is a method on instances of type value. Okay, so Go has methods like C++ has methods, but Go doesn't have objects. So you can declare a method anywhere you like. And in fact anyone can declare a method of a type as long as they have access to the underlying type. You don't have to declare them together with the type itself. All right, now what does the closure look like? So remember the closure, it has the prelude before we define the closure inside of makeValue that defines the channels, creates them and assigns them to the value we're going to return. What is the work of the actual closure look like? Well, the first thing we do is we declare inside the closure an instance of the confined state. In this case, it's just an integer. This copy of that integer is only visible within this closure. No other function can name it directly. The only way any other function can name it is if we pass it and appoint her to to that function as an argument which is exactly what we're going to do. So we're going to range over the request channel. Remember I said last time range just blocks waiting for something to come on the request channel. When something is on the request channel it pulls it off. It's going to assign it to the a request variable, a rec variable. It's going to keep doing that until the channel request is closed. When the channel request is closed that for loop drops out. Question. Yes. Is the channel request closed when done is called? So when done is called, it closes the request channel. Close can only be called on a writable channel. But it turns out done has a writable copy of that channel. So, all this does is it pulls one of these requests off the channel. That request is itself a function. That function takes a pointer to an integer, so we pass it the address of value. That function returns something of type any. We take the return value of type any and we stuff it in responses. and that's it. When the channel's closed, we fall out of the request loop. That exits the go routine. Everything is done. And what happens? Exactly what we expect. Counter's initial, counter, each counter prints its initial value. Actually, interestingly enough, some counters got to do some work before counter three printed its initial value. All of the counters make roughly equivalent process. Three ends up starting last but finishing first, which is kind of curious. But just coincidental, then counter one finishes, then counter two finishes, then counter zero keeps going until it's completely done and when it's done, the value is four million. Just like we expect. Question? So on the previous slides, the for loop will wait until the requests channel is closed and it'll just keep waiting and not continue running the code below it. Right, so what the for loop does is it blocks reading from the channel. When somebody writes to the channel, it pulls off whatever that is and instantiates an instance of a request and assigns to it. If the channel is closed, instead of pulling the request channel off and assigning it to AREC, it just terminates the for loop. So that's what range does. Range is a really convenient idiom in Go. So if you have an array, you can range over the array without declaring indexes and all of that other stuff. And you can just pull things off the array and you'll pull off the index and its value through with each iteration through the loop. And when you've walked through the entire array, it just falls out. Same thing's true for slices. The same thing is true for maps, which are just hash tables. The same thing is true for channels. Yeah. And also, so from last lecture, I think you mentioned read and write only are only for references to a channel, but the underlying channel itself is always bi-directional. Correct. But closure is a thing that is applied to the channel itself. Correct. Not a variable. Correct. Okay. Yeah. All of the operations on the channel apply to the channel itself, not the reference to it. That's correct. Alright, now, this is pretty weird, and I will admit up front, this is pretty weird. If you haven't seen something like this before, it's a little bit hard to wrap your head around. I encourage you to wrap your head around it, because it's a really powerful way of expressing the problem. The other way of expressing this problem, you'll see the example code that one of the IAs wrote, we'll put it up later today, which is instead of just defining the closure to be this generic loop of functions, you define the closure to take a single argument that encodes all of the possible arguments to all of the possible functions with a tag at the beginning and then you switch on the beginning of that tag then you call the appropriate function based on the tag switch. Which you can do, and it may be a little bit more clear the first time you build one of these things. So you can build project one either way. Starting with project two, we want you to do it with function passing. but you can do it with sort of method invocation on project one if you would prefer. You can even write it both ways, project one is not that long. So you could write it as the method invocation way first and then rewrite it as the function and you can do that anytime. You don't have to do it for correctness. So, and the reason I say this is that Historically, I'm not promising I'm going to do this. Let me make it clear, I'm not promising I'm going to do this. But historically, I have often asked a question on the midterm, what's the most interesting thing you've learned about Go so far? And I often ask a question on the midterm, what's the piece of the class that's been the most challenging so far? Again, not promising, but I have done it in the past. So last semester was the first semester I taught confinement with function passing instead of method invocation. Prior to last semester, almost universally, the answer I got from what's the coolest thing you've learned about Go so far was, man, this confinement thing is really cool. I hated it at first, and then when I tried it, I loved it, it is so much easier than using mutexes. Totally cool. And then the thing that was the hardest to wrap their heads around was usually the next thing we're gonna talk about, which was linearizability. Last semester, much to my surprise, the thing in the midterm that everyone said was, not everyone, but a solid plurality of the students said that was the hardest thing to wrap their heads around was confinement. And I think the reason is that I'm teaching it now is this function passing idiom instead of method invocation. And again, that's two different things. Now you're learning about the confinement idiom and you're learning about function passing along the way with generic tights, all of which is new. So I get it. So that's why project one, if you want to build it with the method invocation way and we'll give you the code for the confined integer using method invocation so you can see what it looks like, you can decide. But over time, I'd like you to internalize both of these ideas, because they turn out to be very, very powerful, because by the end of last year, people were really happy with it. But it took longer. Okay, that's fine. I'm not going to begrudge that. I would not be surprised if it plays out the same way. So let's talk a little bit about what it costs. So if we build a version with mutual exclusion, I talked about this last time, it's about half a second that's and it's a little faster than the fine-grained data passing version because the fine-grained data passing version uses blocking channels which insists that you always do a context switch for every single operation the mutex version probabilistically does a context switch at every lock drop but it isn't guaranteed to so there are some times when the same context gets to run a couple more times which takes advantage of some cache effects. So the difference between fine-grained data passing and mutual exclusion is that mutual exclusion sometimes gets lucky with cache effects and so it can run a little faster. But it's doing the same amount of work. The confinement problem now has two handoffs for every operation instead of one handoff for every operation. Right before we had a go routine passing an integer to another go routine. That's one handoff per operation. Now, I'm sending a function to a go routine, performing the operation, sending the value back. Now, there are two handoffs per operation. And, as you would expect, because there are two handoffs per operation, instead of one handoff per operation, it takes roughly twice as long. Okay, what does twice as long It means on my machine, 300 nanoseconds per invocation. And again, that's in the context of blocking IO. We're probably doing a handful of these every time we read something off the network. We sent something over the network, it took milliseconds. Adding an extra 150 nanoseconds to something does not bother me. Now, this could turn out to be a throughput limitation. If it's a throughput limitation, then and only then would I rewrite it using mutexes. Because getting the mutex stuff right is harder. So it's still cheap relative to IO, but in exchange, well first the counters don't have any idea that they're concurrent. You can call them sequentially or concurrently and you don't need to change how they are written at all, which is remarkably cool. They just work correctly because the abstraction you're passing them builds concurrency in under the hood, right? So that's the first cool thing is that now lots of things just don't even need to know that any of this stuff is concurrent at all. It just works. Just like channels just work. A value just works. No matter how many go routines are touching it. It's all fine. More importantly, you cannot make a concurrency mistake with a value, because only one go routine can ever access it. It is exclusively owned and referenced by one running context. Because it's exclusively owned and referenced by one running context, It is impossible to forget to acquire the value, to forget to call lock, or to forget to pull something off a channel. It is impossible to forget to release the value. It's impossible to forget to call unlock, or it's impossible to forget to put the value back on the channel. And it's impossible to use the value after you've released ownership of it because you never had ownership of it. And this is why, historically, a large number of 491 students, by the end of the class, say wow, that confinement thing, really cool. Because in exchange for a little bit of extra boilerplate code, which gets pretty easy to write, you're really not writing much extra. All of the bodies of the methods you would have had to have written anyway, and just surrounded them with new text. The only extra thing you're writing is this little teeny tiny piece of code that reads things off channels and writes back. That's it. The rest of it's the same. Now it feels like more because it's different, but it's not actually different. You just have to think about it the first time you write it. So really you're writing just a teeny tiny little bit of extra code. you have one extra go routine running in your function, and there's a little bit of a latency penalty. Most code is not performance sensitive, but bugs exist everywhere. So this idiom allows you to write your code in a way that makes it harder for you to make mistakes in exchange for 150 nanoseconds per operation. I will pay that every day and twice on Sundays. Question? Is this working? You want an operation that depends on two concurrent values? You know in mutexes, you have to require the locks for both of them, and sometimes the deadlock can be fired down in the wrong order. Is there any issue? So, for confinement, so if two different things have access, So, I own this and you own that, and I want that and you own this, it's possible that we'll get stuck, right? The same thing can happen with confinement. If this confining go routine wants to operate on this confined state, and this confining go routine wants to operate on this confined state, they can run into each other. We will run into this in project two. There are two ways to solve it. One is, always have it go in one direction, which turns out to be the same solution that you would use using mutexes, right? The canonical solution to this problem using mutexes is to apply a total order on any mutexes that are held at the same time and always acquire them in that order. This works the same way it's just that the requests always flow and you have a directed acyclic graph of requests and requests only flow along the graph. The other way to do it if you really need something to go mutually is to have one of them be asynchronous you can do that if you don't care about the result. Either solution will work in project two for the problem that it has, right? So one of the things you can do is instead of instead of calling the method synchronously you call the method itself in a go routine because you don't care how long it takes and you don't care about the return value. Okay, no problem. Other, so part of, part of why there's so many similarities to all of this is fundamentally we're solving the same problem. And you kind of need roughly the same principles to solve the same problem. But you can use different mechanisms that encode the principles. This mechanism encodes the principles in a way that's harder to make mistakes. That doesn't mean you don't have to think about it, right? You still have to think about it, because there are higher level issues that you can sometimes run into. But it eliminates some of the lower level problems, and then you just don't have to think about that stuff. There's an equivalent version of this, see how I can tell this without having to teach like three lectures of 482. One of the requirements of Project 2 and 482 is that you be able to handle exceptions correctly. If you've done 482, Project 2, you know the answer, you know what I'm talking about. But, if you run out of memory, that throws an exception and you have to be able to handle that correctly without leaving a lock stranded. If you build your locks using a particular idiom that we introduced in 4.82, which is RAII, you get that for free. The thing that in Go that's equivalent to that is defer, right? One of the cool things about defer is that defer runs whether the function that that it's inside returns or throws an exception. No matter how that function exits, the defers always happen. So if you acquire dynamic resources, you always release them via defer to guarantee that they're going to be released whether you return or throw an exception. RAII is the same mechanism in C++ kinda sorta. So I had a student walk into my office with a bug, project two, and I'm looking at the code and I'm looking at the bug and I'm looking at the test case they're failing. And I'm like, oh, they're leaving a lock stranded when an exception is thrown. Immediately followed by the thought, I never thought about that. How is it that my solution works? And the reason my solution worked is that I happened to build it with RAII because I rebuilt it, I hadn't done it before, and I just didn't think about the exception thing. And it turns out you don't need to think about the exception thing because RAII just transparently handles it for you. So, even if you're me, with a broken brain, it still just works, right? That's what this is about. You don't have to, you just eliminate having to think about a bunch of problems. So while you're running a code, if there's a problem that you just forget to think about, that's okay, because you can't break it. I'm a big fan of having my own stupidity be unpenalized, right? And that's kind of what this idiom allows me to do. Other questions? All right, let's go ahead and take a two-and-a-half-minute break. When that clock says 11-11, we will return. So for this, is it just like we want one smart part of the actual data to be all that fast? Yep. And then these methods just pass a function to the node. I know it would be... So the counter uses an instance of this value, so it requests the other values, or they're Okay, so anyone else can send me these. Okay, I'll do that. It's like a five second question. All right. So let's talk a little bit about this value type because there's another advantage that this idiom provides us, that turns out to be important. So what if value has concurrent callers? And in fact, in our example, there are concurrent callers because there could be four counters running at any one time, all of which are trying to add at the same instant. Well, if they're concurrent callers, every single caller can block when making any request on a value. Because they have to wait for anything else the value is doing to finish. If any callers exist, they are admitted to the closure that owns the enclosed copy one at a time. So there might be a bunch of callers calling the add method. One of them will be allowed in, and another one won't be allowed in until this one is done, right? because the request channel is unbuffered and synchronous. So, and I don't read from the request channel until after I've responded to the prior request. So, everybody else who's waiting for this value is going to block until the current method is done executing. And that admitted caller executes its operation all the way to completion, and then it's done. Now, what's interesting about done is that normally we can't say anything about when two different go routines execute some operation because their rates of execution are different and unknown and variable. However, the time of the completion of an operation is agreed upon. The time at which an operation starts may not be, so I may call add and block for 20 seconds while the value is doing other stuff. And the value won't start my operation until that 20 seconds has elapsed. So the caller and the callee may not agree on the time at which the operation starts. The caller may see the operation start earlier because the caller may block. However, the caller and the callee agree exactly on when the operation ends because the response channel is unbuffered and synchronous. So if I'm the enclosing go routine and I return a response to the caller, that response is blocked until the caller receives it. And the sending of the object and the receiving of the object are guaranteed to happen at the same time. That turns out to be incredibly important because both agree on exactly when the operation ends. So the order of operations on a confined value is defined by the order in which operations end and all observers can on what that order is. Everyone agrees on the order of events on a value. And it's because of this synchronous connection on the response channel. So now we have a very clear model of a confined value for callers. All of the operations occur in the order of completion. And there's always a well-defined set of events that happened before this one, and a well-defined set of events that happened after this one. And everyone agrees on what those events are, without having to think about it. So the caller can treat any call to one of these things as if it is entirely sequential, because that's how it's implemented. And it doesn't start until all of the before operations have completed. Again, because it's a synchronous channel and the confining go routine will only do one thing at a time. None of the after operations will start until after this one completes, which means that all operations are guaranteed to see all before effects and all operations are guaranteed to be visible to all after operations. And everyone agrees on what the before set is and what the after set is, and no one has to sort of sort out confusion. That's incredibly powerful because now this thing behaves just like an atomic instance of a thing that doesn't have overlapping operations because that's how we built it. Question. So this system, it doesn't prevent deadlock, right? Correct. But neither does mutual exclusion. So if you want to shoot yourself in the foot, you can use either gun. No problem. Yes? I have a question about multi-node systems. Yeah, we're not there yet. Would we also use channels to communicate across? Well, we use networks to communicate across multi-node systems, right? Networks have different behaviors than this, and that's where we're going next. So checks in the mail. Non-negligible latency. Because they have non-negligible latency, exactly right. So we would like, so we're going to build a server. Just one server, one copy of the server that's going to do one thing at a time. Just like we have one Confining Go routine that has one copy of the data that does one thing at a time. And we would like to build that server in a way that provides a similar set of guarantees to all of the callers that there's one order in which everything happens and everyone agrees on what it is. And the problem is that that is not easy. And in fact, doing that even for one server with everything that's synchronous, still requires us to think at least a little. And that's what Project 1 is about. And the reason it's hard is that, well there are two reasons, this is the first reason. The first reason, which is the one you were alluding to is that clients and servers cannot agree on the duration and importantly, cannot agree on when an event ends. And that's because the client's duration is always longer than the server's and the client's perception of end is always later than the server's. Because an operation from the client's perspective lives from the point at which I send a request until the point at which I receive the response. The server's impression of the duration is the point at which it receives the request until the point at which it sends the response. Now, just like the confined Go routine example, the caller might block on the send long before the receiver is ready to receive it. But they agree on when it's done, because the done happens synchronously. But messages always take measurable time, So the client's duration is always guaranteed to be longer, even if nothing else is happening in the system. It doesn't require other things to be happening for this to be true. With the confined value example, if nothing else is happening in the system, the caller and the colleague agree exactly on the duration of a call. It's actually not quite true, but fudging a little bit. But because messages take measurable time, there's always a longer period on the clients and that means operations can always overlap from the perspective of the clients even if they never do on the perspective of the server. So that's the first problem why this model of having a very well-defined sense that before and after is not easy. The second reason why things that, it's hard to provide a before and after, is that failures exist. And because failures exist, even if we have one server that does every operation synchronously, one at a time, there's still an opportunity for operations to overlap. And the way that happens is the following pattern. I have two clients, Foo and Baz. Foo issues operation bar, the server responds, but the response gets dropped. While that response is wending its way through the network to be dropped, Baz issues operation quucks, the server responds, Baz gets the response, Baz goes on its way. Foo doesn't get the response because it got lost, So, it reissues the request bar. That's not a second request, it's a duplicate of the first one. And from Foo's perspective, it's only one operation. The fact that it had to send an extra request is immaterial because it's the same thing. It's no different than if the first request had been delayed. From Foo's perspective, those are identical and they're indistinguishable. What this means is that the operation bar and the operation quacks overlap at the server and the client. And there's nothing you can do to prevent this. You cannot build a server, even a single server with no replicas, that does everything sequentially, that doesn't have to think about concurrent operations. You're just screwed. Yes? So the server waits for a response from one of the clients. Yeah, okay. So, how do I know you got the response? Mom, I sent you a message back. How do you know I got that response? Yeah, that makes sense. Yeah, right. No, this is the problem, right? This is why, remember, one of the fundamental truths of distributed systems is you will never know my exact mental state because it always takes time for me to communicate it to you. That's just the deal, right? And because that's just the deal, this is why, I mean, this is why this course is so much fun. Because there are inherent challenges to just the way we think things work, right? It forces us to think about the world in new and completely different ways. And I, look, I'm gonna tell you a secret, and I usually don't, actually I usually do say this, but it usually happens later. So, the person who designed this course left, took a job at USC, Southern California. I'm a Berkeley grad, so I will not tell you what I refer to the University of Southern California as, but you can probably figure it out. But I still like Harsha, he's a great person, even though he chose institutions in a way that I find questionable. I'd never taught this class before, so we needed someone to teach it. I'm like, well, okay, I guess I go where I'm needed at this point in my career. I had never read the Paxos paper before I taught this class. I didn't read it until May, and I first taught it in September, right? But teaching this class has forced me to re-evaluate how I think about computing, and I first taught it when I was like 52, right? That's pretty neat. So instead, let's talk about what our model for distributed operations is going to be. Distributed, in a distributed system, there are going to be a set of N events, and those events are collectively issued by a set of observers O. An observer submits an event and gets the response to the event back. And by getting the response back, they've observed that the event has occurred. So every observable event has a beginning and an end. The event has to occur some point between the beginning of the event and the end of the event. The problem is we don't know exactly when, so I send the server a message saying please add 2 plus 2. At some point the server will add 2 plus 2 and get 4. They will eventually send me the answer 4, but I can never know exactly when those two things got added. It's just some time between the beginning and the end, and in fact if you look at system with the right perspective, every single thing it ever does is like this. You begin executing an instruction, you end executing the instruction, you don't know exactly when the the transistors change. It changes somewhere between the beginning and the end, right? Now it must appear, our goal is to build the system in a way that guarantees the illusion that every event happens exactly once, and that the set of events happens in some total order. If that illusion were preserved, then whenever any two observers were trying to figure out the order of two events that they both were able to observe, they would agree on the order in which they happened. That turns out to be really important, because events are not ordinarily reorderable. And I haven't used this example yet, I'll use it when we start talking a little bit later in the course, but if I deposit an amount to my bank account, and I pay interest on the balance in my bank account, the order in which I do those things gives me a different answer. So I would like to agree with the bank on what the order of those things was, And in fact, I would like different branches of the bank to also agree on the order in which those things happen. So our goal is to provide this appearance. Now we can't do total orders because we have messages. We can only do partial orders. And it might turn out that there's more than one actual order of events that could satisfy this illusion because no observer can see every single event I can't see your events directly I can only see mine the server can't know the beginning and the end times of any events only when it receives and sends them right so there's no observer there's there's no one who can sit above the entire system and observe of what actually happens and say this is the order. All we get is the collective set of observations and whatever order happens has to occur, however we satisfy events has to happen in a way that no two observers observe two events happen in different orders. All right, so this boil, we can boil this down to two different kinds of operations. There are operations that modify state and operations that observe existing state. We're going to call the former writes and the latter reads. Now some writes depend on existing states like if you're doing an append, that depends on existing state but it's still thought of as a write. So all of the right operations, for this to work, all of the right operations in the world must be totally ordered. There must be one order of rights and everyone has to agree on what they are. Because if that's not true, then an observer that observes in between two rights could see the fact that some people think it's one order and other people think it's the other. So there must be a total order of rights. Now there might be many possible correct orders that you choose for that total ordering of rights, but ultimately you can only do one of them. Eventually you have to decide the order in which you're writing to values. Whatever you choose, you have to make sure that no observations can violate this chosen total order. Now, who can observe a right? There are two ways rights are observed. If I'm an observer that asks for the right, when I get the right is completed message, that's equivalent to observing that the right has occurred. Because remember, the event has to happen somewhere between sending the request and receiving the response. Now, it turns out you don't have to build systems this way, but we will. You could actually build systems that the response is, yes, I will eventually get around to performing your right. But that's not strongly consistent in the way that we want. We want the observers to know what the order was. And if I tell you, yeah, I got your right, I'm going to get to it. Well, that doesn't tell you the order in which they happen. We want the observers to know that. So the observer who requests the right, and when they get the response to the right, they have observed that the write has occurred. It's also possible for any observer who reads that key, if they read the value that was written by this operation then that observer also observes that the write has occurred, right? So there's gonna be some point the write request comes in, we're gonna do some work, at some point during the work we're going to say that the write occurred. If a GET comes in and can read that write before the final write response gets sent, which could happen, we can build our servers that allow concurrency. There's nothing that says we can't, and in fact, in project one, you will. After that, you won't have to, but in project one, you do. In project one, you have to allow reads and writes to happen concurrently. So, if a server who reads the key who reads the written value after it, quote, happened, well, that reader might learn that the write happened even before the person who asked the write learns that it happened. Nothing wrong with that. Question? Maybe this is a stupid question, but can we look at the server's timestamps in the response? Ha, ha, ha. How much time was left at the end of the Western Michigan game after the second play? Well, that's what the Big Ten replay office said. What did NBC say? There were no seconds left. Yeah, so it turns out that if you have two clocks, they will never precisely agree. I mean, they might, but that's just happenstance. We're not going to talk about why just yet, but and we're going to have multiple clocks and in particular the observer owns the clock. So if the observer's clock and the other place this happens in courses not named 491 is when your laptop's clock is a little bit slower than the autograder's clock. Professor Noble, I submitted my project when my clock said 11 59 and the autograder or said it was 1201, it's tough. Too bad, you're late, right? So, if there's only ever one clock that we're all ever gonna talk about, then yes. However, the point of distributed systems is to not rely on the failure of one thing. Because if we were gonna do that, this class is like three weeks long. We'd be done at the end of today. Okay, not very interesting, I mean you can be, but I'm not stopping after this week. Okay, so there are two rules for observable rights. The first rule is the permanence rule. If an observer observes that a right has happened, no later observer can observe that the right hasn't happened. That seems obvious, and it seems easy, and it's not. In fact, this is one of the bugs you can make in Project 1. It's pretty easy to make. Then there's also an order rule. If any observer observes by reading, and all of these guarantees are just about one value. There's only one key. If any observer observes the right A to the key, followed by changing the value of the key to B so the value of the key maybe was empty it becomes A and then it becomes B and I observed those two transitions no other observer can observe the key go from empty to B and then A because then they don't agree on the order all right those are the observation calls now that seemed that all seems very straightforward but it It turns out there are some weird consequences to it. So let's start with a very simple example. There are two clients, one key. The initial value of the key is zero. The first client issues a put, changing the value of the key to one. The second client issues a get, getting the value of the key. There are three possible timelines. The first possible timeline for the server is that it sends the response for B before it sees A's request. So it has no idea A is ever going to happen. And if it has no idea A is going to happen, the only possible answer is zero. The other possibility is that it sees the B request after it sends the A response, and for reasons we're gonna get to, if I sent A the response, Let's say even A received the response. That's really how I probably should have named this. If a server sees B's request after A sees its response, then A knows the write has happened, B also has to reveal that the write has happened. What if A and B overlap? So the clients send them, they arrive at the server at roughly the same time. what must the answer to B to A to B be? Yes. Zero to one. Exactly. So if they overlap, they can be either zero or one. Not a problem. And that's because the server, when it has concurrent operations, is allowed to decide what order the operations will happen in. So if it has both of the operations in hand, it can decide to serve the get first, in which case it's zero, or it can decide to serve the put first, in which case the get must field one. This is one of the things that's also troublesome about this class, is that there are many instances in which the answer to what must this operation be is, I don't know. It could be these three things, all of which would be correct. That is probably a situation in which you're uncomfortable with. Because you probably haven't written many programs in which the actual correct answer is, I don't know. But it's not this. It's only one of these things. But I don't know exactly which one. And in fact, if you read the test cases in project one, we're already giving you test cases that are non-deterministic. There isn't a single answer that is correct for some of those test cases. They're a family of correct possible answers, and as long as you give me one of them, you're right. All right, same scenario, but now we've got three clients. We're going to start the key at zero. A is going to issue a put. The change is the key to one. B and C are each going to issue get. And we're going to imagine that the gets do not overlap with each other. So the gets are completely separate in time. but the PUT overlaps with both of them. All right, now what happens? Well, the first GET could return either 0 or 1. For the same reasoning that the GET in the previous example could have returned 0 or 1, because if we just ignore that the second GET exists, we already know that the answer to the first GET is 0 or 1, and at the point we're deciding on the first get we don't know the second get will ever happen but if the first get returns one we have to give the illusion that each operation happens exactly once at a total order so if the first get returns one then the put operation must have happened before the point at which the get operation happens because from the external observer's point of view everything happens exactly once in a total order so that means that there's some the put we imagine the put maybe happened here and the get maybe happened here if that's the case what must the second get return has to be one because the later get can only happen somewhere between this point, and this point, no matter where we choose on that red line, it's after the point we chose for the put. All right, now, but the first get we just said, could return either 0 or 1. If the first get returns 0, then the put must have happened after the point at which the get occurs. So we choose some point for the get, we haven't committed to where the put happens yet we just know that it happens after that and since we haven't yet committed to when the put happens we just know that it's after that line somewhere the second get can return either 0 or 1 because it's just like the first example that we saw both the put and the get are still to be determined about when they actually occur and so we can pick either one And it depends on the relative order of the put and the second get. So the bottom line is once either a concurrent get returns a new value or a put completes, then subsequent reads must give you the new value in the case in which only one put occurs. If there are more than one puts, then a subsequent read must get the new value from this put, or if there are later puts that happen, it could return a later value. But it can't return an earlier one. So the basic idea is that the writes form a total order and we can imagine that total order as the timeline of the evolution of the key. Every time we read a value, that sets the minimum time at that point. We can never go backward in time. So once I've revealed to you the sixth value in the sequence, I can never answer with the fifth value or an earlier value. I have to either answer with the sixth value or a later one. If I read a later one, that forces time to move forward again. Likewise, finishing a put is equivalent to reading the value. So if I finish the PUT, that also forces time to go forward. Okay. That seems so far so good. Questions? Yes. PUTs and GETs, the server does them all atomically? Doesn't have to. Okay, so it could be done automatically. I mean, well, the PUTs appends are the ones that don't necessarily have to work that way. However, they can overlap. They have to appear as though they occurred in total order. You don't have to implement them atomically. You can implement them concurrently. They just have to appear as if they occurred in a total order. And in fact, you can't implement them atomically because failures occur. All right, so that means when a PUT completes, that advances us along the timeline. What do we mean by the PUT completes? Well, let's talk about what our choices are. What's the earliest point at which a PUT could have happened? What was the earliest point at which we could say a PUT completed? And the earliest point is the moment that the right becomes safe. Safe means survives failure. Now the specifics of what survives failure means can depend on exactly how we're building our system. In some cases it just means it needs to be in the memory of two things, in some cases it means it needs to be in the memory of a majority of things, in some cases it needs to be on disk somewhere, in some cases it will mean it needs to be on disk in a majority of places. Depends on what we're building, right? So safe depends on the specific failure model. However, in the case of a single server, under the 491 failure model, which remember failures are unbounded but finite, we lose in-memory data but retain any memory stored persistently, safe means that the update is stored on the server's persistent storage. So there's some point we issue a disk write or a flash write or some other write to persistent storage at some point between issuing the write and getting the response, the data is actually on the storage in a way that if the machine fails it'll still be there when it comes back. So at that point, the point at which the data becomes safe is the earliest we can say the put completes. Because if we say the put completed before then, If the machine fails between that moment and the moment the data is safe, we lose the update. So that's not good. So if you observe a put before it's safe, then it could be undone by a failure and that's not allowed. And remember, observers could also be concurrent readers, not just the writer. Like if it fails before it's safe, the writer won't believe it's done, but a concurrent reader might have. So we have to prevent concurrent readers from believing the right has happened before the right is safe. Okay, that's the earliest we could choose. What's the latest we could choose? The latest we could choose is the moment that it's observed by any other node. Now there are two ways for that observation to happen. The client that issues the put receives the response or some other client that issues a GET receives a response with that PUT value. As soon as one of those two things happens, one of those two observers receives the message that externalizes the fact that the PUT occurred or communicates the fact that the PUT occurred, that's the latest point at which we can say it occurred. How can the server know when that occurs? In fact, how can the server even know if it occurs? Isn't it never guaranteed, because you don't, even if the client receives a response and the client were to send like an acknowledgement, that acknowledgement could still be lost. Yeah, so, exactly right, it can't. And in fact, even if you were gonna send me an acknowledgement, it's time. I won't receive the acknowledgement at the point you received the response, I will just know that it's later. So I can't choose that time because that's already after the time you've known. I can't use your clock because NBC and the Big Ten can't agree on what a second is. Right? So there's just no way to do that. That's because messages can be arbitrarily delayed. The server cannot know how long it takes to arrive. It could be a very, very, very, very, it could be epsilon time, measurable but epsilon, which is small. It could be very, very large but finite. Messages can also be dropped. The server won't know a response has dropped until and unless it's told by the receiver that it got the message that tells it it wasn't dropped and that message might also be dropped. So there's just no way. You can't do it. So what that means is servers treat responses conservatively. If a response can make an update observable, we say it externalizes that update and the server assumes that any such response, the first such response that externalizes the update can successfully be delivered and will be delivered with negligible delay. There has to be some delay, but it's smaller than, like, whatever number we put on it, we can cut it in half. That could be the number. Well, that number could be cut in half. Well, that could be the number. That number could be cut in half. This is one of those physics paradoxes. If you keep getting halfway to the wall, you'll never actually touch it. But there's always, we could always get closer. So, if we assume that it's seen at the point we send it, well, that's going to be earlier than absolutely necessary. But it can't be late. Because the message takes measurable time, Now, the receiver will not receive the message until sometime after we send it. How long? Don't know. Could be half that much. But it's not zero, just could be half. So if we assume it was zero, then we can't be late. And so this is probably going to be the last bit that I get to for today, but this is what this looks like. So we have a client, and then this is the server. The server has both memory and disk. The client sends the put to the server that puts right out of the network into memory. There's a little bit of processing that happens on that thing. Then we send it to the disk with a disk write. That's blocking I.O. because we want to make sure the write actually makes it to disk. If we're not waiting for the write to actually make it to disk, we don't know that it's safe. At some point the disk does its thing. The disk controller gets the thing and sends it to the platter. The magnetic field on the platter flips bits. Eventually, the data is on the disk reliably. At some point in that process, the data is safe. At some point after the data is safe, we get the response to the disk right, and that's the point at which the server knows that it's safe. Before the server knows that it's safe, if it's serving a concurrent read, it has to give you the old value. So it has to give you the old value until the disk write returns. Because until the disk write returns, I don't know that it's safe. Then there's some more processing. Then I'm going to send the result to the requester. Once I issue the send message, from that point forward, any concurrent read must return the new value. What about between those two? I can do either one, and when we come back we'll reason about why that's true. Actually let me see what I'm doing. Oh I actually have just enough time to do this. So the old value is possible because just like before we were talking about reads and writes overlapping, we could choose a later point in the interim for the write because we haven't externalized it yet. So we could return the old value by deciding that the right happened later. The new value is also possible because we could choose a time as early as the disk right returning to be the point. If that's the point we choose, we return the new value. No problem. So in this interim, we could choose. We can't choose before here. We can't choose after here. We can choose anywhere in the middle. And when we come back, we'll see the consequences of that.