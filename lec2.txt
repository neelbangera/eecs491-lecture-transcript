All right. Welcome to the first substantive lecture. So last time we talked it was really just the introduction of the topic. Talked about what is a distributed system, informally it's a group of nodes cooperating to provide a single service, a little bit more formally it's a group of nodes that are separated, communicate only through messages and through no shared state, those messages have delay, and they do not share fate. What shared fate means is that if any particular event causes one of the machines to fail, it does not necessarily cause the rest of them to fail. That one's actually a little bit loose. The place where shared fate can show up. It's not so much physical failures. You put machines in different data centers so that if the meteor hits one of the data centers, the others probably still run. But if they all have the same bug and experience the same input, then they might all fail corresponding to the same input. The primary potential advantages to doing this are first resilience, so rather than have a service that is only as good as one machine's ability to not fail, you have a service that appears to never fail at least in some limits. And scale, you can do something bigger than you can do on any single machine. The main challenges for this from the point of view of designing systems to work correctly are that they are concurrent, inconsistent, and ambiguous, all of which will turn out to be pretty significant challenges for us in the semester. And finally, any distributed system has three goals that are in tension with one another. You can always give an answer. You can make sure that any answer you give is correct, or you can be fault tolerant, and you can't be all three of those at the same time. And the short reason why is if you are, if you only have access to a minority of the machines providing the service, they can't know the genuine answer to any particular question because the majority that you can't talk to might have changed the answer. If that happens, the minority either has to not answer or answer in a way that's potentially incorrect. And we'll formalize this a little bit more as we go. Today, I'm going to sort of give us a model for computation. What is the 491 model for computation? How do we think of computation? Also, the 491 model for communication, and importantly, what is our model for failures for both of those? And it's going to be quite specific. It is not fully general. There are gonna be some failures we do not consider, some of them because they're really easy to fix, others because they're really hard to fix, and we just won't have time this semester to get to the solution to that. We'll also give a motivation for why any particular distributed service has to be concurrent in order to be reasonably efficient. And then we'll talk a little bit about why concurrency is a challenge. Some of you probably know why concurrency is a challenge, but I don't assume that you do. Before I get started on today's material, does anyone have any questions about anything we've talked about up until this point? Okay, before we get too far into this, I want to give a warning or a caution, maybe it's a caution, not a warning, for anyone who has previously taken EECS 482. First, our communication model is different and actually simpler. So the communication model we have in 491 is somewhat less difficult to deal with than the communication model in 42, so that's kind of nice. Second thing, goroutines are not exactly threads. You don't know what a goroutine is yet, we will soon, and we'll talk about them. But our unit of concurrency, our unit of concurrent execution, is not exactly threads and in fact goroutines are a little bit, you might worry about creating a thread, you should never worry about creating a goroutine. Maybe that's the simplest way to put it. The most important difference is that as we think about how to deal with concurrency in this class. Again, if you haven't taken 42, this might not make any sense to you and that's totally fine. But if you have taken 42, go routines should never share mutable state in this class. Ever, ever, ever. You might wonder how you could ever get anything done. We'll see how to get something done. But what's cool about that is with no shared mutable state, you do not need locks. and there are higher level synchronization mechanisms that the language provides, which I actually think are a little bit more pleasant to work with. And so I'll conclude with Epictetus' quote, it is not possible to learn what you think you already know. All right, let's start talking about the models. So the 491 node model, each working node in the system whether it's a server or a client makes progress at some unknown variable rate. So the node is working, it makes progress through its computational efforts. It will eventually make progress, but we have no idea how fast. And even if it was fast before, it might not be fast now. If it's slow now, it might be fast later. Each working node does not share any state with any other nodes. They don't have a file system in common, for example, that they can both talk to. If there is some common thing, it's some other distributed service which, in turn, works this way. Every single working node has its own persistent and transient storage. Persistent storage is flash or disk or something else that survives failures. Transient storage is just memory or a cache or something that doesn't survive a failure. Every working node is connected to all of the other nodes by something that we'll call the network. Our node failure model is that nodes can temporarily fail. When a node fails and recovers, any persistent data, which might have stored on disk or flash, et cetera, and is known to have been stored there, survives the failure, but any transient data that happened to be in memory is lost. So if you didn't write it out to some persistent storage, it isn't guaranteed to exist in the next second because failures can happen at any particular time. A little bit more on this question of failure. Everything in our system in 491 is what's called fail stop. So every node, every component in a node, every processor, every disk, block, every memory byte, every element in our system, either if you ask it a question, and it will either answer correctly or not answer at all. So if you ask a processor in 491 what two plus two is, it will always tell you four or it just won't answer. It will never tell you five. The alternative is called Byzantine failure. The reason why is for something called the Byzantine General's Problem, which is the problem that poses this question. And it's a very interesting model, right? And in the Byzantine model, you can ask the processor to add 2 plus 2, and it might give you 5. And that's, or you can store a 6 in some memory location and read it back and get a 7. So, Byzantine models, Byzantine failures are very, very interesting and completely beyond the scope of what we're going to do in 491. If you want to learn how to solve Byzantine failure, probably need to take 591 or something else, or go off and read, which is actually allowed. Finally, failures. When a node failure happens, the duration of that failure is finite but unbounded. Finite means it will eventually recover. Unbounded means what we have no idea when. So we never consider permanent failures. So if we have a node, we never consider the case in which that node dies and never comes back. And we do that because persistent data persists. disks, we just can't know when the failure will end. Now it turns out if you're like, probably not 470, maybe it's 3, maybe it's 473, if you're someone who builds actual hardware, you might realize that persistent data that never fails actually turns out to be a hard problem, we're just going to assume that problem is solved for us. If you write something out to the disk, it will always be there. Networking. So that's our model for computation. What about our model for communication? And we're going to have a very abstract model for communication. If you really want to know what's going on, take 489. But for the purposes of 491, the 491 network model is that we have this abstract network and this network connects all pairs of nodes. So every node is connected to every other node in the system. Each pair of nodes are connected by two completely disjoint one-way connections. So the way A talks to B is completely separate from the way B talks to A. Every node might send message to any other node at any particular time. Like everything else in the 491 model of computation and communication, communication paths are fail stop. If I send you a message, that message will either be delivered correctly or not delivered at all. So, in particular, we do not worry at all about someone who's maliciously trying to tamper with the network communication. You want to think about that, go hang out with 388. The community, any communication path can fail for arbitrary but finite periods of time just like our nodes, so network communication failures are unbounded but finite which means eventually if I am trying to send you a message, I will eventually be able to do it sometime before the heat death of the universe, but I don't know when. Also, in addition, just like we can't predict the rate at which computation unfolds and computation might unfold arbitrarily slowly, messages can be delayed for arbitrarily long periods of time. A message that is delivered will eventually be delivered in some point in the future, but we can't predict how far in the future that point might be. Finally, every single one of these paths is completely independent of all of the others, which feels a little weird, but turns out that if you can build a system that does this correctly, you can build a system that does weaker versions of this correctly. And so there are two really important consequences of this. It might be possible for A to send messages to B at the same time that B cannot send messages to A because they're asymmetric. It is also possible that A might be able to send a message to B, B might be able to send a message to C, at exactly the same time that A cannot send a message to C. So they don't obey the triangle inequality. Just because I can find a path through the network that is successful, doesn't mean that this particular communication will be successful. Now, this is a much, as I mentioned, this is a much cleaner model than the network model we usually deal with when we're talking about systems courses, because in general a network message could be lost, I send a message, it isn't delivered, it could be arbitrarily delayed, it could be corrupted, I could send you a message, you could receive something different than the thing I send you, that could be by accident or by malice, in general. It could be duplicated. I can send you one message. You could, you might receive it twice. I could also, in general, reorder messages. I can send you the message A, then I can send you the message B, and you might receive them in general in the order BA. Now it turns out some of these are relatively easy to solve, and we are not going to think at all about the bottom three. We will not worry about corrupted messages. If I send you a message and you get it, you can believe it was the one I sent you. If I send you a message, you will only receive it at most once. You might not receive it at all, but you won't receive that specific message twice. And you can assume that if I send you any set of messages, the subset that you receive will be received in the order that that subset was sent, okay. Now it turns out again that the solving duplicated and reordered messages is actually pretty straightforward. Solving corrupted messages in the face of a malicious adversary turns out to be harder. Again you want to worry about hang out in 388. I will sometimes use the word process and node interchangeably. So when I say the word process I usually mean a process running on one server node. Typically processes and nodes map one to one. There one node runs one process so a node more formally a node is a machine on which one or more processes may run a process is some computational component of the system which has a more formal definition that isn't actually that important to us in 491 a process may itself be concurrent we'll talk about what that means but I'm just gonna put a pin in that for now it turns out if you two processes, two 482 processes running on one node, you can model that as two processes on two nodes. And it kind of just works. The node of the network model collectively will be referred to as the X491 failure model. So if I ask you for example on an exam question, under the X491 failure model, what I is a world in which nodes and networks are fail stop, failure independent, which means one failure doesn't have anything to do with any other part of the system, and failures are always finite but unbounded. Questions so far? It's a pretty straightforward model. It is a little bit cleaner and easier to think about than maybe the model you have in your head for how some of this stuff works. and that's just to make everything else possible. Now, one of the things about network messages is that they're very, very unpleasant, right? The only thing I am capable of doing with the network is sending a set, sort of a chunk of bytes to you. And those bytes are completely uninterpreted by the network. So there are some pretty interesting questions about how to deal with this. One question is, how do we pack data into messages? So if I want to send you the contents of a structure, how do I turn that into a message? Particularly because the two machines on each end of a communication may represent integers completely differently. One might be big-indian, the other might be small-indian. How does the receiver getting one of these messages just turn it into some data structure that it cares about. How do, if I send you a request and you send me a response, how do I match the message you send me in response to the request I sent you? All of these turn out to be kinda interesting problems. However, they're not that interesting and they're not interesting enough for us to understand deeply how they work in 491, at least here. There are some other distributed systems classes that do spend, like, a week or two on this. If I were to do that, I wouldn't get to some of the cooler stuff that I really want to do. So, instead, we use something called a remote procedure call abstraction. So, again, we're not going to talk deeply about how to build remote procedure call. We're just going to assume that remote procedure call is a thing that exists for us to use. Now, in a remote procedure call, a network interaction of I asked a service to perform some operation, the service performs that operation and tells me the operation is complete, possibly with some arguments and possibly with some results. That network interaction, request with arguments, perform an operation, return with results, is roughly equivalent to a function call, which is why it's called remote procedure call. It's a procedure call that happens to be remote. Now for a remote procedure call, the way this works is that a caller, running on some distinct node, wants to invoke some operation F on a call E, which is usually some other distinct node. It doesn't have to be a distinct node, but typically it is. The caller supplies the arguments and supplies space for return values. Then the caller calls the function F on the call E, using some magic underlying mechanisms that the RPC subsystem knows how to do. And then the caller waits for the callee to respond. The callee performs that operation F, then the callee returns its result to the caller through the network, and then that result will be deposited in the space the caller provided. And then the caller gets to continue with the results of the operation in the space that it provided. Programmers, yes, question. What do you mean when you say that the caller's providing space for return values? Yeah, so when I invoke a remote procedure call, instead of receiving a return value like I would from a function, I have to give it a buffer that it will fill with the return values. And this is all in the caller side process? It's all in the caller side process, yeah. Programmers mostly understand this because it looks mostly like a regular function call. Now that assumes programmers understand function calls. We're going to just take that one as a given question. So going back to returning the results, when it returns the result, is it returning the requests with the response space filled in? You can think of it that way. It turns out it just sends the response back because you don't need to send the request back. I know what the request was because I sent it to you. So it's giving it like here's how much space that the response should take up and then... Right. So what basically happens is I send the arguments, there's something called marshaling, which converts the arguments into a network representation. The arguments arrive plus the name of the function we're trying to invoke at the server. It unmarshals the arguments into a local structure, then passes those arguments to the function that was named. It takes the return value, it marshals that, sends it back. The RPC system unmarshals that and puts it in the buffer that the caller provided. Yes. So, is this similar to like RDMA conceptually in that the colleague writes the response directly to the caller's memory? Um, I would not, RDMA, so RDMA makes me a little nervous because I don't, I'm not able to actually directly modify arbitrary memory on the server, but you can think of it as I'm just passing you the arguments, just like we pass the arguments in any way, right? If I was calling a local function I would pass the arguments and I would do that either by pushing them on the stack or sticking them in memory somewhere and giving you a pointer. Here, instead of pushing them on a stack, I'm sending them to you in an envelope. And then you open the envelope, you go, oh, here are the arguments to the function, I know how to call that. There are some complications, unsurprisingly, because it's not just a function call, it's a remote procedure call. It's not exactly a function call because there are two things that an RPC has to provide that a normal function call wouldn't have to do. The first is I need a way to find the callee. If I'm calling a function that's part of my address space, I just need to know the address of the function. This function isn't part of my address space. It lives on some other machine, and I definitely don't want to be mucking directly with that other machine's address space. So I need some way to get to the callee. And then I also need some way to communicate to the caller that the remote procedure call failed. And that doesn't mean the thing that I was invoking, the semantics of that operation were violated and the operation says the operation failed. It means the act of invoking the call and getting the response failed. So let's talk about each one of these in turn. How do we find the callee? Now, this next little bit is really just so you kind of know what's going on. For the purposes of 491, you won't get into this deeply. But the first thing to know is that nodes are implicitly named by a network address. Network address is usually a hexadecimal number. There are two different versions. There's one that's a short version, one that's a long version. I'll use the short version because it's easier to type. For example, Lamport's short version address is 141.212.115.203. And I checked, it still is true. So, if I want to send a message to Lamport, I have to know this, I have to somehow know this number. And it turns out there's a mechanism in the internet that if I give it the name lamport.ecs.umich.edu, will spit me back that number. That's called the domain name service. Now, Lamport itself might have more than one network endpoint. It's not just everything sent to Lamport goes to all of the processes running on Lamport. There could be different processes or several different endpoints within one process. We won't really talk too much about different processes on OneNote in this class, but we absolutely will have one process that has more than one active network endpoint at a time. Now the endpoints are named by the address, which we already know, followed by a port, which is a 16-bit number, typically. So one way to name this is lamport.eex.umich.edu, colon, 80, turns out to be the network endpoint for the web server that's running on Lamport. So if you send an HTTP request to that, you'll get back an HTTP response. Because there's internal mechanism that converts this name to the number, and then the network knows how to get stuff where it goes. Again, 489, lots of cool stuff in that. You can think of an address in a port to being very similar to a building address and a room number within that building, right? So a building has a street address. The one we're standing in now, Byster, the street address of Byster is 2260 Hayward Boulevard, 48109. Every room within this building has a room number. We're currently standing in 1670. My office upstairs is in room 4713. So if you want to send me a physical piece of mail, which does still exist, you wanted to send me a physical piece of mail, you would address it to that street address and that room number and it will come to my mailbox on the third floor. I do not get mail for Professor Haldeman who is on one side of me. He's in room 4717. I do not get mail for Professor Grubbs who's on the other side of me in 4709. So, in fact, I have no idea unless I go looking in Professor Haldeman's mailbox, which I don't do. So the way this works in our RPC mechanism, so call will be the way we invoke an RPC. Call will name an endpoint and the function on that endpoint. So, it's going to be a method of a type. So, we'll name type.method. I'm going to pass some arguments. I'm going to give some room for some results. And the way this works is the args are going to have some particular type. The results will have some other particular type and we'll call. So, the end point is the way to name the point on the colleague that is going receive this request and act on it. For the purposes of 491, the endpoints will generally be opaque. You will be handed them and you will just use them. You don't need to know what they mean. You don't need to look inside of them. If you want to talk about how the infrastructure works, we can talk about it. In fact, I'm giving you the, you can actually see how we build it. So have fun with it, but you don't need to think about it and I recommend that you don't spend too much time on it unless you're really curious, which is fine. But don't feel like you need to understand how all of this works. It's not that important. It just is. That's not that interesting. The more interesting thing is how do we discover that the RPC failed. Now there are two notions of failure. One notion of failure was that the operation was invoked incorrectly. Now in 280 or 281, here's a little pop quiz, 280 in particular, how do we specify how to create, how to invoke a function correctly? What are the ways we say that? Yeah. We have the function header and the requires clause. Excellent. Yes, the requires modifies effects clause. So, how many of you remember the phrase requires, modifies, effects? You remember hearing it at some point. How many of you have ever actually written one that you didn't have to write for a class? Okay, good, a couple. Do those. Future you will be very happy that today you wrote those for future you. But in particular, the requires clause says, if you're going to call this function, these are the rules. If you don't follow the rules, that's on you dog, cannot help you. And in fact, I hope we still say it this way, in 280 we say if a caller violates the requires clause, you are allowed to do anything and it's legal. You could launch the missiles if you want, fine, go for it, have fun. In 491 we don't quite do that, we do validate the inputs, we don't trust the but we'll play a little fast and loose with this because we don't think callers are malicious, so we don't worry too much about that. However, I'm thinking about a different notion of failure, not invoking the operation incorrectly, but the act of invocation failed. And this is going to be completely new. So when I say the RPC failed, I don't mean you called the operation incorrectly, or the operation had a bug, I mean the act of invoking the operation did not succeed. That's communicated through this return value error, which I'm pretty sure is just a boolean. It's true if it worked, it's false if it didn't. I think I mentioned this last time, but we're going to say this over and over and over and over again. There are three sources of failure in an RPC. The first is the the message that I'm sending to the callee to invoke the operation gets dropped, so the callee is unreachable from the caller, so the request we say in that case we say the request failed. The callee is unable to respond, the callee has failed, we're able to deliver the message to the callee or for whatever reason the callee is or has failed. It's also possible that we got the message of the request to the callee, the the callee got the request to perform the operation, but the response failed, the caller is unreachable from the callee, and the response fails. And that can happen because the network paths are asymmetric, right? Our model of the network is me sending you a message is completely independent from you sending me a message. One might be possible while the other one isn't. When a failure happens, You cannot tell which of those three things is at fault. It is impossible. All you know is you didn't get an answer. All right, how does this work under the hood? I'm just gonna, again, this is more to satisfy curiosity than anything else. You don't need to know about this part because we're not gonna build it. Yes? You previously said that responses can take arbitrarily long. Yeah. How do you know the difference? So that's a fourth option. So in that option, the RPC hasn't failed. We'll come back to that. And the answer is you don't, but the RPC system eventually just gives up. We'll come back to that a little bit. So when there are no failures, caller opens a new connection to the port for each call. That new connection is independent of any other connections that any other callers may invoke. The caller waits for the response on that connection. When the response arrives, that connection is closed. So if two different clients call an operation sort of simultaneously, they will arrive on different connections, and they'll be independent from one another. While the caller's waiting, so how do I know that this, so this is the way we know that it hasn't failed. So, this is the inverse of your question. So, how do we know that it hasn't failed yet? And the way we know it hasn't failed yet is that the caller run time sends a periodic heartbeat message. So, I send, like maybe I'm asking you to compute some really, really expensive thing. This is going to take a really long time. While I'm waiting, I periodically send, hey, are you still around? And if you're still working on it, you say, yep, still good. So, if the heartbeat arrives at the callee, which means the network to the callee is is alive the runtime of the callee responds if it's able which means the node hasn't failed if the response arrives at the caller we know that there's no failure yet it doesn't mean it can't it just means it hasn't yet so what happens if something does fail if something does fail the network path from the caller to the callee fails, the callee is down or literally just too busy to answer. The network path from the callee to the caller is down under any of these conditions, that heartbeat won't succeed. And if the heartbeat doesn't succeed, if there are enough responses to the heartbeat that don't arrive, then I declare that there's an error. Now remember, these failures are transients just because the RPC fails now doesn't mean if I try it again right away it will fail again. It might succeed or I might wait a little while to try and that's an open question. One of the things that is very, very, very, very, very important, if the RPC fails, in other words the call returns false, Yes, the result structure is garbage. It is not necessarily what you put there when you started. You have to assume the result structure is completely undefined. If you look at it and try and make a decision based on it, and the RPC has returned false, you are doomed. Please don't do that. Questions so far? Yes. So for the heartbeats, it's the caller sends a heartbeat, and then also expects a response. So I asked you to do something really, really difficult, and it's going to take you all day. Hey, you still working on that? Yeah. Hey, you still working on that? Hey, you still working on that? Yeah, right. So as long as I keep getting a yes, I assume things are going to be okay. Now, that doesn't mean that, like, if the other side is stuck in an infinite loop. I'm not going to know that. But again, we assume these things don't have bugs, right? So we assume they're fail stop. Other questions? Yes. Does the, do the Harvey messages use the same network path? Yes. Well, in our model, it's not actually true because the actual network is allowed to route any message however it cares to. Pays your money, takes your chances. Yes. If the drug receives a heartbeat, how can we know if the employee is too busy or is down? Well, eventually we just declare what looks like it's probably down. We don't know for sure that it is. We just know we haven't been getting answers. Which brings me to the next question. So this is also the question that you were asking. So what about failures that aren't, right? Remember, there's a fourth option if I don't get a response. And that fourth option is, it will eventually arrive, it's just really, really slow, right? So I send a heartbeat request off the network and it's also really slow and I don't get the response the amount of time I kind of assume that I'm gonna get it. This is why you can spend two weeks building RPC, because then you have to start thinking about, well, how long do I wait for a heartbeat? How many heartbeats do I wait for? All kind of interesting questions, but not interesting enough for me to care about this semester. So the RPC system can declare a failure even if the underlying call will eventually succeed. It just doesn't know it yet because things are really, really slow. If that happens, the RPC subsystem throws away the late response so if the RPC subsystem has said this call that I'm waiting for an answer for I haven't heard anything back it's been forever I guess it's dead and then two minutes later the response comes back I look at don't care dropped which means very very very very slow things get coerced into failures so you don't have to worry about if the RPC subsystem says it has failed, you do not have to worry about later handling a response. You will never see it at the callee. Looks like there are some questions. I see furrowed brows. Yes. Is assuming that the results are garbage after RPC fails just something that we do in 491? Or is it? No, it's probably the case that it depends on the exact sort of specification of the RPC system you're using. But if you assume it's garbage, you can't be wrong. It's like we said, the results are transient, so like a second later, like the RPC could succeed. But if the second later the RPC could succeed, you've already declared it failed and the result structure is garbage. And in fact, the RPC subsystem, if it gets the response, it's not going to fill in the response buffer. It's just going to drop it on the floor, in part because I don't own. So one way to think about this, and this will make much more sense if you've ever written Rust code, the RPC subsystem only owns the buffer during the call. Ownership passes back to the caller. And this is also a 280 thing when we talked about at most once. We can talk about it a little more offline. But the basic idea is that I own the buffer before and after the call, the RPC subsystem owns the buffer during the call, which means if the call returns, the RPC subsystem is no longer allowed to muck with it. That would be bad. Okay, something to notice. When there are no failures, a caller opens a new connection to the port for every call. the caller waits for the response on that connection and then when the response arrives, the connection is closed. What do we do if lots of clients are calling at the same time? We kind of have two choices. One choice is, well, we take them in some order, we let the RPC system sort of decide what order we're gonna invoke these operations in. So if we've got six client calls that all arrive roughly at the same time, The RPC system will just pick one of them and hand it to us. We'll invoke, we'll deal with that one. And then when we're done, we'll get the next one and we'll handle that one. And then we're done, we'll get the next one. The other way is to invoke those operations all at once. So we have three clients calling the add operation. The add function will be called three times all at the same time. Okay, the first thing is called sequential. So that's the sequential model of, and the second thing is concurrent. The sequential one is much easier to think about. It's also really bad, and we won't do it. So the server, the RPC subsystem is built so that calls can be invoked concurrently. Why? Well, so the reason is, if you're the caller or colleague, what has to happen to satisfy a request? Well, you've got to read the request off the network, which depends upon the speed at which the request is being sent to you, right? Because I don't have to say, hey, could you do this operation for me? I can say, hey, could you do this? So the request doesn't have to come quickly, it could come slowly. Then I have to perform the request, which is gonna probably involve some computation. It might involve a read from the disk subsystem or flash memory. It might involve a synchronous disk write. So if I'm going to tell you that I did an operation, and that operation has to survive failure, It has to have made it to disk and I have to know that it made it to disk. That's what I mean by synchronous disk, right? I send it to disk and I don't, I don't, I wait there until I know it's there. Maybe I have to communicate with other internal nodes. You're asking me to do something, I have to ask some friends to do some stuff, and I have to send some messages within the data center to do that. And then I write the response back to the network. So, reading the request off the network can be slow. Again, because if the request doesn't arrive all at once, then I have to sort of wait till I get the whole thing to build it. If I'm doing a disk read, that also can be and is usually slow. It might be sitting in the disk cache, in which case it's pretty fast, but it's probably slow. Synchronous disk write is always slow. Nothing you can do about that. It just is, and it doesn't matter if you're using Flash instead of disk. It's not as slow, but it's still slow. And communicating with other internal nodes, that's also slowest because again, all of our communication takes measurable time. Measurable is not instantaneous. Instantaneous is fast, not instantaneous is slow. And then I write the response network, that's usually fast, because usually I just have to give it to the memory of the operating system and then it sends it out when it can. Now if the server has multiple cores or processors, which they all do, in fact I'm going to bet none of you own a device with a processor that has only one core. Maybe if you own a smaller Raspberry Pi or some other embedded processor, maybe. But your phone has multiple cores, everything you own has multiple cores. There are reasons for that, the reasons are really kind of 470 reasons, we're not going to get into than here, but that's the way we've been able to make hay as a field for the last 20 years. So if the server has multiple cores and processors and I'm only doing one of these at once, all but one of these processors is not serving clients and one of them is mostly just sitting around waiting for stuff to happen. Okay, how long are we sitting around waiting for stuff to happen? So these are from 2010-ish, I think. So these absolute numbers are not exactly correct, but they're famous. These are Jeff Dean's numbers to know. We're gonna hear Jeff Dean's name a lot. Jeff Dean is one of the primary, was, he left Google very recently, but he was one of the primary systems architects at Google for their distributed system stuff. So really what I want you to think about is the relative costs of these things. Because the relative costs of these things, while not exactly constant, then they are in more or less the same order. In fact, I think they are in exactly the same order. And mostly the ratios are pretty close. So, you know, if I, an L1 cache reference, that's pretty fast, designed to be pretty fast. If I have to actually go out to memory, that's quite a bit slower than hitting it in the cache. That's why we have a cache. Reading one megabyte sequentially from memory, that takes a little bit of time, but you know we can strobe it and there's the memory designers have done some stuff to make that that pretty fast but if we have to do a round-trip to in the data center that's more than reading a megabyte of memory so you can kind of draw this line below the line is IO sending a round-trip within the same data center waiting for the disk to do something reading something from the disk sending a packet from California to the Netherlands, and back. So all of that stuff is I-O. The stuff above the line is not I-O. Well, sending is I-O, but it's not blocking I-O. Sending I-O is just, it's really just a memory operation. But blocking for things, that's super expensive. So over time, you'll sort of internalize the relative cost of these things. and you just kind of want to know them in your bones, right? But the thing for now that you really need to know is that any time you might block for IO, waiting for something to come to you off the network, waiting for something to come off the disk, waiting for a response from somebody else in the data center, waiting for the disk to complete or write, any of those are blocking IO. Whenever you have to do blocking IO, it can take many, many, many, many cycles. Many, many, many, many, many cycles. While the processor is blocked, if it's not allowed to do something else while it's blocked waiting for IO, the processor cannot do anything else. So the conclusion is, if we're ever blocking for IO, we're gonna spend ridiculous amounts of this very expensive processor doing nothing. So we would like to arrange for something else to happen if possible. Now how you arrange for the something else to happen if possible is 482. We're just going to assume it works because we're handed a language in which it just works, which is super convenient, right? So if we can do this arranging, which we know how to do through 482 and related mechanisms, then we can do other things, which means if I get a bunch of calls all at the same time. Some of those calls might be waiting for IO, but other calls might have computation they could do. And even if I only have one core, I can sort of do them all at the same time. And I have more than one core, so I can really do them all at the same time. Okay. So, coming back to this, We're handling each connection separately and concurrently, so the implications of separately are the port named in the call is used to create a connection, the connection is a new distinct unnamed endpoint. So when two clients call to the same endpoint, they will get new connections. It's kind of like dialing a phone. When you dial the phone, you get a connection to the side. So all of these things can be handled distinctly because from the server's point of view they all look like distinct invocations. The implications of concurrently are that the function that implements any remote operation plus any of the functions it might call can have more than one copy of that function running at exactly the same time. If you have never seen this before, now you've seen it before if you've taken 482, you've seen kinda sort of it before if you've taken 484, maybe 485, I don't know if 485 does this or not. There are a couple classes where we talk about this, but if you've never seen this before, it is totally bizarre. If you have seen it before and you think you understand it, you don't. And that's, I'm saying that largely because there are even times when I get surprised by what happens. This is just not happening. And the problem is, this is not how brains, our conscious existences work. Now brains do work concurrently, it turns out. My brain is doing all kinds of crap that I have no idea is actually happening. It's running my heart, it's running my lungs, it's doing all kinds of stuff, but but all I know, I am experiencing the world as a sequential set of events because my conscious attention can pay attention to one thing at a time. So our model for the world is that it is sequential, fundamentally, from the point that we were born. And unfortunately the world is just not that way, it is concurrent. I also want to say a little bit about nomenclature, about this, the words, people often use the word concurrent and parallel synonymously, they do not mean the same thing. Concurrent is a property of the system, and a system is concurrent if it's possible that two things can happen at the same time. Parallel is a of an execution. Parallel means two things do happen at the same time. So concurrency is a property of your design. And it's an expression of what is possible. And you can tell whether or not a program is concurrent by reading it. We sometimes say if you can tell something about a program by just reading it, we call that a static property. You can determine it statically. Parallel is a property of the execution of a concurrent program. A program that is not concurrent can never be parallel, but a program that is concurrent is not always parallel. Sometimes it is and sometimes it's not. It's a property of the execution and it's an instance of what actually happens in a given execution of the program. That can only be known dynamically and it can be different from one execution to another. So even though you have one concurrent program and you give it exactly the same inputs in exactly the same sequence, it can still end up executing differently. Because the rate at which the processor moves is not known and there are all kinds are good reasons for that. I think I say this later but I'll say it now. This is also sometimes called non-deterministic. So a program is deterministic if you give it the same inputs and it always gives you the same outputs. A program is non-deterministic if you give it the same inputs and the everything you've ever written is non-deterministic because if you include the time in your output, that changes from execution to execution, but not in an interesting way. This stuff is a little more interesting. Now, one of the things I'm gonna make the case for this, this is a go belief and I subscribe to it. Concurrency can improve the and the readability of your program. Alright, what's cool about objects? We talked about this on Monday. Why are objects in a typical object-oriented programming language interesting and cool and helpful? I'm sad. Yeah? Yeah, what's cool about objects is any state that's private to the definition of that type can only be seen by the methods of that type. And then if you have a structural invariant, then man, you get to go to town, then you get local reasoning. But at the very least, you know that if someone is mucked with some private piece of state in your object, it has to be one of the methods, it can't be anything else, right? So what's cool about objects is that they encode the data with the operations that are possible on that data. And that allows for local reasoning. What's happening? Sammy has returned. A long lost GSI has returned. That's surprising. Dig it. All right, I dig that. Welcome. So what's cool about objects, they encode the data of the operations. Concurrency can do the same thing. So instead of methods, which are passive things that get invoked, we can have actors which are active things that respond to stimuli and those actors can be encoded with the data they act upon and this also can allow for local reasoning so one of the things one of the ways that we're going to use this in this class is that we'll have a single actor encoding some piece of state that gets used by these invocations and since only one actor can touch it it is never shared that may not be immediately obvious That's why that's cool. It is extremely cool. All right, so how does this work in a programming language? So, we'd like to be able to build a programming, a program that is concurrent. How do we build a program that is concurrent? Well, we need the programming language to support concurrency in some way. So, here's one way to do that. And there are other ways, but here's a way. So, think about how a function call works. You put the arguments in some known place, You execute the jump to subroutine instruction with the address of the subroutine. The called function executes knowing where the arguments are. It places the return value or values in some known place and executes the return from subroutine instruction which goes back to the callee right after the invocation which knows that the results are magically where they're expected to be. So one way to think about this is that the caller and the callee are just completely independent, like they are in RPC, the caller invokes the callee and waits for it to be done. What if the caller didn't have to wait? Now if you don't wait, you're not gonna get the results in the obvious way, because if you're not waiting, the results aren't there, because maybe the thing's not done yet. But that means it can go off and do its thing while you do your thing. In Go, there is something called a function modifier keyword, the Go keyword. Go can appear before any function call, but it has to be like you can't say Go two plus two. You have to say Go the function that adds two and two together. Go can appear before any function call, and what it means is Go do this, I'm not waiting for the answer. If you call a function this way, the invoked function is referred to as a go routine. Now, we also say that it's called asynchronously. If it were synchronous, we would wait for it. If it's asynchronous, we don't. The arguments to the call are evaluated at the time of the call. The actual go routine is going to start running at some point at or after the call. We have no idea when. And we have no idea how fast. We just know that it's going to start. But the arguments are evaluated at the time of the call. If the function we're calling has returned values, they're going to be completely ignored. So there's going to need to be some other way for that thing to communicate back to us if it needs to. And maybe it doesn't need to. That's OK, too. All right, we've been going for almost an hour. It's a little longer than I probably should have. Let's take a two and a half minute break. when that clock says 11-28, we will return. I'm like trying to think of examples. How many times do you have been in both? Well, if it's not stateful, how do you do a bank balance? I'm going to deposit to my bank balance. Does this make sense? Even if it's on one machine, that's still stateful. If I put my current balance, I'll get enough. The part we see wouldn't be safe in that case, it would just be someplace in this or that way. Well, I mean, we got active. We got active. It's like, we're controlling this structure. It's not your fault. It's not your fault. It's not your fault. It's equal based on the state of the entire system. Yeah, yeah. It's fine. It's fine. It's fine. It's fine. It's fine. It's fine. It's fine. It's fine. It's fine. Okay. So let's take a look and see how this might be used. So I have this function called count. Count takes an integer by reference, so count takes two integer, two arguments. First argument is value, which is a pointer to an integer. The second argument is a label, which is a string. This is all Go. It'll read pretty straightforwardly if you've used another imperative language, either C, C++, Java. Familiarity with any of those, this should probably sort of kind of make sense. So first thing I'll do is my initial value, print it out, the initial value of this number I've been handed is a value and there's a label because I might have different counters that are running and they need to each say which counter it was, then you know for zero from zero to a million add one to the value. So at the end of this I expect the value to be one million bigger than it started. Pretty straightforward, boring, not particularly useful function. Now, if I were going to call it synchronously, this is what that would look like. I'd start with some initial value for the value. Let's say I'm going to do this four times. And for four different times, I will count, so I will call count counter zero, then count counter one, then count counter two, then count counter three. What I would expect to see is if the initial value is zero and then it's a million, then the initial value is a million, then it's two million, and so on. And in fact, if I run the sequential version of this program, that's exactly what I get. No surprises so far. Straightforward 183 stuff. If I make one change to the main function, so instead of calling count synchronously I call it asynchronously. So now what this says is, in this loop four different times, launch the count function with a reference to the variable val, But don't wait for it to finish. Just launch it. And when they're launched, I'll print done and what the value is. Okay. If I do that, that's kind of weird. So somehow the first thing that I printed is done. The value is zero. So that's the first thing that gets printed. Which means the main go routine is at least at that point here. And then, counter 3's initial value is 0. Well, counter 3 wasn't the first one I invoked, but okay, I mean, I guess. And then, counter 1's initial value is 0, counter 0's initial value is 0, counter 2's initial value is 0, and then the program just stops. That's a little weird. Okay. The first thing to know about Go is the main method is distinguished. When the main method, when the main Go and main runs in its own Go routine, it can start other Go routines, which in turn can start other Go routines, but it's it runs in its own. When the Go routine that main runs in ends, all of the Go are terminated with prejudice. So, whatever other Go routines have been launched by the main Go routine are stopped. If you've taken 482, this kind of makes sense because main exiting ends the process. The process is the unit of resource allocation, so you recover all of the resources. Those are all those are just resources. You have the extra Go routines. However, the The other four Go routines did all do a little bit of work before the overall Go routine ended. So between this point and this point, which is kind of the end of the program, so between printing out this message and actually having the Go routine end, which doesn't look like there's a lot there, but the printing the message is distinct from ending the Go routine, those are distinct events. The other go routines did a little bit of work. That's kind of fascinating. So, you know, they all managed to print their initial message. They might have even managed to do some of their work. Don't really know. We just know that none of them got to the point of printing their end message. Some of them might have even incremented some of those values. Could be. So, well, if they got to do some of their work, maybe we should just give them some time. Give them some time to do their work. So one way we could do this is put the main go routine to sleep. Because we probably don't want to print done until we're done. So time sleep says, hey put this go routine to sleep for a second, after a second wake it back up and let it run again. And if there are other go routines that could be doing some work, let them do some work. No problem. Second's probably long enough. I mean we're only incrementing a number a million times. It doesn't take that long. So if I do that, this is the execute and execution of that version. Well everybody prints their initial values, everybody prints their final values, and done prints its final value after a second has completed, and we get 1.3 million and change. Just what we expected, the four million. If we run it again, now we get 1.7 million. That's weird. Not exactly the same. In fact, if I keep running this over and over and over again, some of the initial values won't even be zero. I just didn't print them up. That's weird, right? So, how is it that I keep getting different answers for all of these things? And also, kind of interestingly, the counters don't end in the same order. The first time, the counter zero was the first one to finish. The second time, counter one was the first one to finish. So, something definitely weird is going on, but this is really weird. Like, how all of these things are running a million times. They are all incrementing their value a million times. Yet somehow the final value is not four million. That seems weird. The other stuff kind of makes sense, right? We make sense that the counters finish in some unknown order, because we don't know how long it will take for these things to work. But the fact that they don't add the same number of times, even though they execute the same number of add operations, that's bizarre. Okay, what's really going on? So, what's really going on is we have this line. DREF would take the value that is stored at the memory location named by val, add one to it, store that value back in that memory location. Now, 370 is not a prerequisite for this class. I've thought about changing that. I don't think I need to. So, I'm just going to explain what happens here. All right, what happens here is that a processor does not operate on memory directly. A processor has a little bit of scratch memory inside of it called the registers. And to do any operation, it first has to fetch data from memory into a register. It performs an operation using one or more registers, and when it's done performing the operation, it takes the memory that's in the register and writes it back. That's the model of how computation really works with a CPU oriented system. GPUs are a little different, not going to talk about it, don't care, but a CPU that's how CPUs do everything they do. They read from memory, they perform an operation internally, when they're done performing the operation they will eventually write back to memory. So what's happening in this line is that we load from the memory location to a register, we increment the register and we store the result that's in that register to the memory location val. And if more than one of those things is happening in parallel, this is what that looks like. So let's suppose the value 6 is sitting off in memory, I have two processors. One processor loads 6, the other processor loads 6. The processors independently increment their sixes to sevens, then one processor writes back seven and the other processor writes back seven. So there were two additions formed, performed, but we only have evidence of one because the work that one of the processors has done is basically lost. So, when processor one loads while processor two is loading and they're allowed to load, it turns out that in most shared memory systems the loads are ordered because there's only one memory bus and everything has to go over that. Again, that's kind of like, that might even be 470 stuff. But that's sort of what happens. But then the increments can happen independently and then in some order the stores will happen. So when that happens, one of the increments is dropped. How many are dropped on any given execution? We have no idea. Here it is approximately 2.7 million and change increments were dropped. Here we dropped approximately 3.6 million and a lot. Almost 3.7 million. So lots and lots and lots of these increments are being dropped. So, what's going on? What's going on is that we have absolutely no way of knowing. I did this fast. I cannot believe I'm at this far in this lecture this fast. Really worries me. Oh, well, we'll figure it out. We'll see what you know. We have no way of knowing the order in which the counters start, the speed at which each of those counters run or how the counters interact through value and it's potentially different every single time. And this is called non-determinism. Bad news, good news, how to win big. This is a play on a very famous, how many, let's see if anyone's seen this because I probably none, I'll post it anyway. How many have you, how many of you have ever seen the worst is better argument? Raise your hand. Okay I'll post it. The worst is better argument basically means there are business reasons why building bad software is a good idea. It's a really cool argument and it's titled good news bad news how to win big. I'll post it. It's worth a read. It's very interesting. So this is a play on that bad news good news how to win big. The The bad news is that concurrent go-routines can make progress at arbitrary rates and interleave with each other in arbitrary ways. And in fact, this isn't even the worst of it. There's a third thing that I'll talk about next time, which is not only can they interleave in arbitrary ways, but one go-routine can see the effects of another go-routine out of order. So if a Go routine stores seven to location val and eight to location val2, another Go routine can see the eight storage before the seven storage. So the other Go routines aren't even guaranteed to see the first Go routine's actions in order, which is disastrous. Like that is, I don't know how you write correct programs that way, but you can't. The good news is that the hardware designers are good friends in 4.70, 5.70, 3.70, have seen fit to give us mechanisms in the processor to make some guarantees about how this works. Not guaranteeing everything, but guaranteeing some very specific things. Those low-level processor mechanisms are used in fairly magical and cool ways to provide something called synchronization. Synchronization basically boils down to one guarantee. So I have two Go routines. This Go routine has an event S, or let's say B before. this Go routine has an event A after. Synchronization is a technique that allows us to link B with A such that we're guaranteed that B happens before A, and everything that happened before B in program order is visible to everything in A after that point. So it guarantees that two things happen in a particular order and that they have particular visibility. Now it doesn't say anything about what happens in the first go routine after B and what A can see after B. A only knows it can see everything that happened before B. Guaranteed. So the hardware designers have given us these very low-level mechanisms called acquire and release, actually more accurately release and acquire, plus we play some games in the operating system and 482 will teach you more or less how to do this although we kind of hand wave at the release and acquire bits I think unless we've fixed that. Now the really cool stuff, so not only the good is how to win big, is that Go makes synchronization very easy and by very I mean small values of easy. So it's easy-ish and one of my primary claims about 491 for those of you who have taken 492 and are convinced you know how to solve this problem is that I have a better way because the way I have to solve it in 491 there are an entire class of errors that you can make with the way you do in 482 that you just cannot do here if you use the techniques that I'm going to show you. So that's the that's the deal. Before you go, I do have one other observation. So remember we said these concurrent go routines can make progress at arbitrary rates and they can interleave with each other in arbitrary ways. What does that mean for our good friend's sleep for one second? Yeah? It could actually not be for one second, it could be more or less? We can assume that it's definitely not less than a second. Okay, but it could be more. Well, so the sleep will happen for a second, but we don't know how long between waking up and executing the next thing will be. So, I'm willing to say that the sleep will happen for exactly one second. That doesn't tell us anything about how long it will take us to perform the next action, just that the next action would be able to begin at the end of that second. But there's another problem. what's the other problem yes necessarily take one second to finish yeah exactly so in all of the we don't maybe maybe a second is tons of time maybe not and we don't really know. Every execution I've shown you and in fact every execution I've ever run all of the counters print their final line before the main go routine has exited. I have never seen an execution for which one second was not enough. Does that mean this is correct? You're shaking your head no, why not? Because it might take more than one second. It's possible, and I just haven't found that execution yet. But in the universe of all future executions of this program, there exist executions in which one or more of the counters does not finish before the main go routine ends. That has some very important implications for this class, some good and some bad. The first is that correctness is not defined as it never does the wrong thing when I run it. Correctness is defined as the wrong thing is impossible. That's what we mean by correct here. That's the downside because what that means is you cannot test your way to correctness unless you can test all possible interleavings of the program and you do not have that much time between now and the heat death of the universe. Even for simple stuff. There is some good news about it though. How do I evaluate your programs? Yeah? An autograder. An autograder, what does the autograder do? Go ahead. A finite number of tests. It runs at a finite number of times. I guarantee you that finite number is probably not one. Because you might just get lucky. And I don't want to reward lucky. I want to reward mostly good. It does mean it is possible to submit programs that are incorrect that nevertheless get full credit if their incorrectness is rare enough. We'll struggle with that a little bit as we go, but we'll do that next time. I'll see you then.